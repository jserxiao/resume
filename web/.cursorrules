# 项目：AI 简历编辑器

## 技术栈
- **框架**: React 19 + TypeScript
- **构建**: Vite 8
- **状态管理**: Zustand + Immer（3 个 slice：resume / block / editor）
- **UI 库**: Ant Design
- **样式**: Less (CSS Modules)
- **富文本**: Tiptap
- **导出**: html2canvas-pro + jsPDF
- **拖拽**: @dnd-kit
- **路由**: react-router-dom

## 项目结构

```
web/src/
├── assets/            # 静态资源
├── components/
│   ├── EditorCanvas/  # 画布主区域（含 FreeBlockCard）
│   ├── ExportMenu/    # 导出菜单
│   ├── LayerDrawer/   # 图层抽屉
│   ├── LeftPanel/     # 左侧模板面板
│   ├── PreviewDrawer/ # 预览抽屉
│   ├── RightPanel/    # 右侧属性面板
│   │   ├── BlockDetailPanel.tsx  # 块详情面板（折叠分类：内容/变换/外观/间距）
│   │   ├── FieldEditor.tsx       # 字段编辑器（按 FieldType 渲染）
│   │   ├── FlexboxChildList.tsx  # 弹性盒子子元素列表
│   │   ├── RichTextField.tsx     # Tiptap 富文本编辑
│   │   └── TagListField.tsx      # 标签列表编辑
│   ├── shared/        # 共享组件（ColorFieldInput, SidesInput, ImageUploadField 等）
│   └── Toolbar/       # 顶部工具栏
├── hooks/             # 自定义 Hooks（useKeyboardShortcuts, useAutoSave）
├── pages/
│   ├── EditorPage.tsx              # 主编辑器页面（路由 /）
│   ├── DecorationEditorPage.tsx    # 装饰编辑器页面（路由 /decoration-editor）
│   └── decoration/                 # 装饰编辑器子模块
│       ├── types.ts                # 内部类型（AnchorPixel, EditablePath, GuideLine）
│       ├── useDecoAnchors.ts       # 锚点/路径状态 Hook
│       ├── useDecoSnapGuides.ts    # 辅助线与距离标注 Hook
│       ├── DecoStageCanvas.tsx     # SVG 画布渲染
│       ├── DecoToolbar.tsx         # 工具栏 + 形状选择
│       └── DecoPathList.tsx        # 右侧配置面板
├── store/
│   ├── index.ts       # Zustand store 组合入口
│   ├── types.ts       # Store 内部类型
│   └── slices/
│       ├── resumeSlice.ts  # 简历数据管理
│       ├── blockSlice.ts   # 块操作（CRUD、位置、尺寸、样式）
│       └── editorSlice.ts  # 编辑器状态（选中、缩放、对齐辅助线）
├── styles/            # 全局样式
├── templates/temp1/   # 模板数据
├── types/index.ts     # 全局类型定义
├── utils/
│   ├── constants.ts   # 常量集中管理
│   ├── block.ts       # 块工具函数（对齐、距离计算）
│   ├── geometry.ts    # 几何计算（贝塞尔曲线、形状生成）
│   ├── decorations.ts # 装饰定义
│   ├── export.ts      # 导出功能
│   ├── iconMap.tsx    # 图标映射
│   ├── imageUpload.ts # 图片上传
│   ├── migration.ts   # 数据迁移
│   └── presets.ts     # 预设数据
```

## 核心概念

### 数据模型
- **Resume**: 简历根对象，包含 blocks、groups、colorScheme 等
- **BlockInstance**: 画布上的一个块，含位置/尺寸/样式/字段值
- **BlockTemplate**: 块模板定义，含字段列表（FieldType 枚举）
- **Group**: 分组，多个块的逻辑容器
- **CustomDecorationDefinition**: 自定义矢量装饰，含贝塞尔路径

### 状态管理
- 单一 Zustand store，3 个 slice 组合
- Immer 处理不可变更新
- Store 方法命名约定：`updateBlock*`, `add*`, `remove*`, `toggle*`

### 坐标系统
- 所有坐标为像素值（px）
- 装饰编辑器保存时将像素转为 0-100 百分比
- 装饰编辑器加载时将百分比还原为像素

### 路径别名
- `@/` → `src/`

## 编码规范

### 命名
- 组件文件：PascalCase（`BlockDetailPanel.tsx`）
- Hook 文件：camelCase 带 use 前缀（`useDecoAnchors.ts`）
- 工具函数文件：camelCase（`geometry.ts`）
- 常量：UPPER_SNAKE_CASE（`DECO_GRID_SIZE`）

### 组件拆分原则
- 单文件不超过 400 行
- 超过时按职责拆分为子组件或 Hook
- 渲染函数提取为独立组件
- 状态逻辑提取为自定义 Hook

### 样式
- 使用 Less，类名用 kebab-case
- 组件样式文件与组件同目录

### 导入顺序
1. React / 第三方库
2. Ant Design 组件
3. Store / Types
4. 自定义组件
5. 工具函数 / 常量
6. 样式文件

## 测试
- 测试文件放在 `__tests__/` 目录或与源文件同目录
- 使用 Vitest
- 优先为 utils/ 下的纯函数编写测试

## 常见修改场景

### 添加新的块模板
1. 在 `types/index.ts` 的 FieldType 添加字段类型（如需要）
2. 在 `utils/presets.ts` 添加模板预设
3. 在 `components/RightPanel/FieldEditor.tsx` 添加字段类型渲染（如需要）
4. 在 `utils/constants.ts` 的 `getDefaultBlockWidth/Height` 添加默认尺寸

### 添加新的装饰形状
1. 在 `utils/geometry.ts` 的 `generateShapeAnchors` 添加形状生成逻辑
2. 在 `pages/decoration/DecoToolbar.tsx` 添加工具按钮

### 修改 Store
1. 在对应的 slice 文件中添加方法
2. 方法签名在 slice 的 interface 中声明
3. 使用 Immer 的 draft 模式修改状态
